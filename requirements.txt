# Reproducible environment for the released FPNN–CatBoost/BO–UCB workflow.
# Recommended interpreter: Python 3.10.x
#
# Ax is pinned because the production code uses
# `ax.service.managed_loop.optimize`; its managed default generation strategy
# is version-dependent.

numpy>=1.24,<2.0
pandas>=2.0,<3.0
scipy>=1.10,<2.0
scikit-learn>=1.3,<1.6

matplotlib>=3.7,<4.0
seaborn>=0.12,<1.0

openpyxl>=3.1,<4.0
XlsxWriter>=3.1,<4.0

torch>=2.0,<2.3
torch-geometric>=2.4,<2.6

catboost>=1.2,<1.3
rdkit>=2023.9.5,<2025.0

ax-platform==0.3.7
botorch==0.10.0
gpytorch==1.11

